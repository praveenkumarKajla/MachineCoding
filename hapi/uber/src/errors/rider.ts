import { ErrorResponse } from ".";

export const RIDER_ERRORS: Record<string, ErrorResponse> = {

    UNABLE_TO_FETCH: {
        name: "UNABLE_TO_FETCH",
        errorCode: "R001",
        message: "not able to fetch rider details!",
    }

}